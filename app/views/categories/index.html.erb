<section class="my-container">
  <%= render '/shared/nav_main',title: 'Categories'  %>
  <div class="categories-container">
    <% @categories.each do |category| %>
    <%= link_to user_category_path(user_id:@user.id, id:category.id),
        class:'category-links' do %>
        <div class="category-card bg-primary-light text-primary">  
          <div class="category-card-wrapper">
            <i class="material-icons outlined material-icon"><%= category.icon%></i>
            <h4 class="category-card-title text-primary-light"><%= category.name%></h4>
            <h4 class="category-card-amount text-primary">$ <%= category.total_amount %></h4>
          </div>
        </div>
      <% end %>
    <% end %>
  </div>
  <%= link_to new_user_category_path(user_id:@user.id),class: 'bottom-btn-link' do %>
    <div class="bottom-btns-wrapper bg-secondary">
      <button class="bottom-btns bg-secondary text-secondary app-font">
      Add New Category
      </button>
    </div>
  <% end %>
  </section>

